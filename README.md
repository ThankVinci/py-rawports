# About py-rawports

A composite interface for sending raw data.

## Function

Communicate with different types of interfaces (such as Socket, USB, Serial, FileIO, FileDescriptor...) using common read/write functions.